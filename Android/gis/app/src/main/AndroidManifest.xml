<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ps.leic.isel.pt.gis">

    <!-- Add permission for NFC -->
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/OrangeTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.InitialSetup1Activity" />
        <activity
            android:name=".activities.ProductCategoryActivity"
            android:label="@string/products_label"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.ProductDiaryActivity"
            android:label="@string/products_label"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.ListActivity"
            android:label="@string/listLabel"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.SettingsActivity"
            android:label="@string/settingsLabel"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.AboutActivity"
            android:label="@string/aboutLabel"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.StockItemListActivity"
            android:label="@string/stockItemListLabel"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.RecipesActivity"
            android:label="@string/recipesLabel"
            android:theme="@style/OrangeTheme" />
        <activity
            android:name=".activities.StockItemDetailsActivity"
            android:label="@string/stockItemDetailsLabel"
            android:theme="@style/OrangeTheme" />
        <activity android:name=".activities.AllergiesActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.ListsActivity" />
        <activity android:name=".activities.InitialSetup2Activity" />
        <activity android:name=".activities.NFCActivity">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="ext"
                    android:mimeType="text/plain"
                    android:pathPrefix="/pt.isel.leic.ps:simulator"
                    android:scheme="vnd.android.nfc" />
            </intent-filter>
        </activity>
    </application>

</manifest>